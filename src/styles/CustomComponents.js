import { chakra } from '@chakra-ui/react'

export const Main = chakra('main', {
  baseStyle: {
    height: '100vh',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    padding: '0',
    margin: '0'
  }
})
